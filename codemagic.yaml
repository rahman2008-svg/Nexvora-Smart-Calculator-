workflows:
  android:
    name: Nexvora Smart Calculator Build
    max_build_duration: 30
    environment:
      java: 17

    scripts:
      - name: Grant permission
        script: chmod +x gradlew

      - name: Build APK
        script: ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
